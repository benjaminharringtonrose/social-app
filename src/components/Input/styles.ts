import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    minHeight: 50,
    margin: 12,
    borderWidth: 1,
    padding: 10,
    borderRadius: 6
  },
});

export default styles;