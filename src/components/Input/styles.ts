import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    minHeight: 60,
    margin: 12,
    borderWidth: 1,
    padding: 10,
    borderRadius: 20
  },
});

export default styles;