import { StyleSheet } from 'react-native';
import { Font } from '../../constants';

const styles = StyleSheet.create({
  label: {
    fontFamily: Font.family.montserratBold
  }
});

export default styles;